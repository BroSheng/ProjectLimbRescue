package com.example.shared;

/**
 * Enum for the 'desc' column of Sensor. Obviously not too necessary now,
 * but setting up an enum from the start paves the way for SCALABILITY and makes querying
 * more consistent.
 */
public enum SensorDesc {
    /** Photoplethysmography Sensor */
    PPG
}
